spring.application.name=gymtrack
server.port=8081

# --- MongoDB ---
# MONGODB_URI example (Atlas): mongodb+srv://<user>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority
spring.data.mongodb.uri=mongodb+srv://iskander:Topadmin2005@cluster0.wkuwwi9.mongodb.net/?appName=Cluster0
spring.data.mongodb.database=gymtrack
spring.data.mongodb.auto-index-creation=true

# --- JWT ---
jwt.secret=cTl9S/TRehgyJAYJN5LmVCUHqZqOdVq+rAuJdKO18xgpjQF80MlY+YgfxokBzjod
jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# --- CORS ---
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,https://gymmpilot.netlify.app}

# --- AI Service (Groq / OpenRouter / OpenAI compatible) ---
ai.base-url=${AI_BASE_URL:https://api.groq.com/openai/v1}
ai.model=${AI_MODEL:llama-3.3-70b-versatile}
ai.api.key=${AI_API_KEY:gsk_free_key_placeholder}

# --- JavaMail / SMTP (Brevo) ---
spring.mail.host=${SMTP_HOST:smtp-relay.brevo.com}
spring.mail.port=${SMTP_PORT:2525}
spring.mail.username=${SMTP_USERNAME:9f4310001@smtp-brevo.com}
spring.mail.password=${SMTP_PASSWORD:bskSQRq3hYKdLHn}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=7000
spring.mail.properties.mail.smtp.timeout=7000
spring.mail.properties.mail.smtp.writetimeout=7000

# --- App Mail & Frontend Settings ---
app.mail.from=${APP_MAIL_FROM:jpfdjxymjg72@melbourne.edu.pl}
app.mail.from-name=${APP_MAIL_FROM_NAME:GymPilot Support}
app.frontend.url=${APP_FRONTEND_URL:https://gymmpilot.netlify.app}