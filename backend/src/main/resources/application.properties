spring.application.name=gymtrack
server.port=${PORT:8081}

# --- HTTP Compression (70-85% smaller payload) ---
server.compression.enabled=true
server.compression.mime-types=text/html,text/xml,text/plain,text/css,text/javascript,application/javascript,application/json,application/xml
server.compression.min-response-size=1024

# --- Tomcat Thread Pool & Connection Tuning ---
server.tomcat.threads.max=200
server.tomcat.threads.min-spare=20
server.tomcat.connection-timeout=20000
server.tomcat.max-connections=8192
server.tomcat.accept-count=100
server.tomcat.keep-alive-timeout=60000

# --- MongoDB ---
# MONGODB_URI example (Atlas): mongodb+srv://<user>:<password>@<cluster>.mongodb.net/?retryWrites=true&w=majority&maxPoolSize=50&minPoolSize=5
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://iskander:Topadmin2005@cluster0.wkuwwi9.mongodb.net/?appName=Cluster0&maxPoolSize=50&minPoolSize=5&maxIdleTimeMS=60000&waitQueueTimeoutMS=5000&connectTimeoutMS=10000&socketTimeoutMS=20000}
spring.data.mongodb.database=gymtrack
spring.data.mongodb.auto-index-creation=true


# --- JWT ---
jwt.secret=cTl9S/TRehgyJAYJN5LmVCUHqZqOdVq+rAuJdKO18xgpjQF80MlY+YgfxokBzjod
jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

# --- CORS ---
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:5174,https://gympilot.tn,https://www.gympilot.tn,https://gymmpilot.netlify.app}

# --- AI Service (Google Gemini / OpenAI / Groq compatible) ---
ai.base-url=${AI_BASE_URL:https://generativelanguage.googleapis.com/v1beta/openai}
ai.model=${AI_MODEL:gemini-3.7-flash}
ai.vision-model=${AI_VISION_MODEL:gemini-3.7-flash}
ai.api.key=${AI_API_KEY:${GEMINI_API_KEY:${OPENAI_API_KEY:}}}

# --- Resend HTTP API (Port 443 - works on Render Free Tier) ---
resend.api-key=${RESEND_API_KEY:}
resend.from=${RESEND_FROM:GymPilot <support@gympilot.tn>}

# --- JavaMail / SMTP (Gmail Fallback) ---
spring.mail.host=${SMTP_HOST:smtp.gmail.com}
spring.mail.port=${SMTP_PORT:587}
spring.mail.username=${SMTP_USERNAME:gimpilot411@gmail.com}
spring.mail.password=${SMTP_PASSWORD:ljwulmbdzdopxpmq}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=7000
spring.mail.properties.mail.smtp.timeout=7000
spring.mail.properties.mail.smtp.writetimeout=7000

# --- App Mail & Frontend Settings ---
app.mail.from=${APP_MAIL_FROM:gimpilot411@gmail.com}
app.mail.from-name=${APP_MAIL_FROM_NAME:GymPilot Support}
app.frontend.url=${APP_FRONTEND_URL:https://gympilot.tn}

# --- Cloudinary (Image Hosting) ---
cloudinary.cloud-name=${CLOUDINARY_CLOUD_NAME:dvfrd0ehf}
cloudinary.api-key=${CLOUDINARY_API_KEY:777366458436392}
cloudinary.api-secret=${CLOUDINARY_API_SECRET:kjVQbJ07IMvwzvVHs9Uv819yup0}

# --- D17 Manual Payment Configuration ---
app.payment.d17.phone-number=${D17_PAYMENT_PHONE:+216 21 214 512}
app.payment.d17.recipient-name=${D17_RECIPIENT_NAME:GymPilot Official}